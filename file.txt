A young stupid camel
Found programming GNU-UNIX an easy hurdle!!!
	Ran as Forrest, ran.
that was very slow
And that's not saying much for the faggots!!!
********************
To err is human. To realy foul up you need a computer.
********************
<p>
To <b>generate</b> a table of contents
all the C <b>program</b> files in your
current working directory, use the
<b>command</b>.
<pre>
	$<b>ctags *.c</b>
</pre>
</p>
********************
This is a test with two
or more
lines
********************
This iz a test.
&&&&&iz a test.
********************
now IS THE TIMe ...
********************
#include "stdio.h"
#include "fcntl.h"
#include "uninst.h"
#include "stdlib.h"
#include <sys/fcntl.h>

// Read at most 10MB
const unsigned int MAX_READ = (10 * 1024 * 1024);
/ Size of buffer
const unsigned int BUF_SIZE = (62 * 1024);

// Buffer to be written
static unsigned char buffer[BUF_SIZE];
********************
<H1> Dumb user tricks

At onw univesity the computer center was experience
trouble with a new type computer terminal. Seems
that the professors loved to put papers on top of
the equipment, covering the ventilation holes. Many
terminals broke down because they came so hot that
the solder holding one of the chips melted and the
chip fell out.

The student technicians were used to this problem. One
day a technician took the back off a terminal
/the
********************
teh
the
the

